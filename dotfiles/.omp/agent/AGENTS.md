# OMP Workflow

主代理负责 intent architecture context integration final verification

只在能提高速度 成本 质量时 delegate
优先最低成本且可靠的 worker
困难 不确定 高风险任务升级

task context 必须 self-contained
主代理写入 task.context task.tasks[].task hub.message 的所有自然语言 MUST 简体中文
code command API name path identifier exact error log URL number 保持原样
并行只用于独立 workstream
后续优先复用已有 worker

review 只在 regression architecture security risk 值得时触发
